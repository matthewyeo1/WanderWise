import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:ww_code/settings_page.dart';

void main() {
  /*
  testWidgets('SettingsPage UI test', (WidgetTester tester) async {
    // Build the SettingsPage widget
    await tester.pumpWidget(MaterialApp(home: SettingsPage()));

    // Initial check: Ensure ProfilePage is displayed by default with "Save" button
    expect(find.text('Save'), findsOneWidget);

    // Tap on General bottom navigation item
    await tester.tap(find.text('General'));
    await tester.pump();

    // Check if GeneralSettingsPage is displayed after tapping
    expect(find.text('General Settings Page'), findsOneWidget); // Example assertion

    // Tap on Reminders bottom navigation item
    await tester.tap(find.text('Reminders'));
    await tester.pump();

    // Check if NotifSystem is displayed after tapping
    expect(find.text('Set Reminders'), findsOneWidget); // Example assertion
  });*/
}
